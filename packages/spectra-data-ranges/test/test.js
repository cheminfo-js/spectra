'use strict';

const spectraDataRanges = require('..');

describe('spectra-data-ranges test', function () {

    it('Something to test', function () {
        (true).should.be.equal(true);
    });
});
